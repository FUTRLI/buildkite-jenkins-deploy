name: Jenkins Deploy
description: Trigger the Jenkins deployer from a BuildKite pipeline.
author: https://github.com/FUTRLI
requirements:
  - bash
  - aws
configuration:
  properties:
    lambda:
      type: string
    job:
      type: string
    iam-role:
      type: string
    parameter_name:
      type: string

  required:
    - lambda
    - job
    - iam-role
